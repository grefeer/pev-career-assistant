"""Company-research service package.

The runtime orchestrates one company-research run (browse + deterministic
extraction); the service layers persistence and validation on top.
"""

from __future__ import annotations
