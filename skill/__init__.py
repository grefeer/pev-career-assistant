"""Skill packages.

Each business skill lives in a hyphenated directory (``skill/job-discovery``)
holding SKILL.md, references, scripts, and evals.  The Python runtime for a
skill lives in the parallel underscored package (``skill.job_discovery``)
because Python module names cannot contain hyphens.
"""
